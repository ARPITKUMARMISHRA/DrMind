import { React } from 'react';
import {
    Box,
    Paper,
    Container
} from '@mui/material';

function Suggestions({ percentage }) {
    return (
        <Container component='main' style={{ width: '100%', minWidth: '100px' }}>

        </Container>
    );
}

export default Suggestions;